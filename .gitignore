# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
serviceAccount.json
*serviceAccount*.json
# Firebase Admin SDK private keys (downloaded from console). These must
# never be committed — rotate immediately if one ever lands in git.
*firebase-adminsdk*.json
*-adminsdk-*.json
reset_admin.mjs

# Firebase CLI cache (may contain project metadata)
.firebase/

# Misc secrets
*.pem
*.p12

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
